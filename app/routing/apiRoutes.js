// =============================================================
// API
// =============================================================

// // api route to view current friends (GET)
// app.get("/api/friendList", function(req, res) {
//   res.json(survey);// Displays tables array in JSON Format
// });
// // api route to see Github Repository (POST)

// app.post("/api/GitHubRepo", function(req, res) {
//   var dateAnswers = req.body; //req.body references body parser npm package
//   // newRes.routeName = newcharacter.name.replace(/\s+/g, "").toLow